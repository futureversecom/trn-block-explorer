query GetTransfers($limit: Int!) {
	balances {
		transfer(limit: $limit, order_by: { block_number: desc }) {
			timestamp
			status
			id
			extrinsic_hash
			block_number
			asset_id
			amount
			from_id
			to_id
		}
	}
}
