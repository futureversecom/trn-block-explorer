query GetTransfers($limit: Int!) {
	balances {
		transfer(limit: $limit, order_by: { id: desc }) {
			timestamp
			status
			id
			extrinsic_hash
			block_number
			asset_id
			amount
			from_id
			to_id
		}
	}
}
