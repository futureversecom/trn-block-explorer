query GetTransferByHash($hash: String!) {
	balances {
		transfer(where: { extrinsic_hash: { _eq: $hash } }) {
			id
			amount
			asset_id
			block_number
			extrinsic_hash
			status
			timestamp
			to_id
			from_id
		}
	}
}
